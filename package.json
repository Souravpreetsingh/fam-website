{
  "name": "fam-mountain-retreat",
  "private": true,
  "version": "1.0.0",
  "description": "Flamingo aur Maina — Boutique Stays in Jibhi",
  "scripts": {
    "build": "cd react-hero && npm install && npm run build",
    "dev": "cd react-hero && npm run dev",
    "start": "npx serve react-hero/dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
